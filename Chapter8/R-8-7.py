# What are the minimum and maximum number of internal and external nodes in an imporper binary tree with n nodes?
# Since 1 <= nE <= 2^h and h <= nI <= 2^h-1 and log(n+1)-1 <= h <= n-1,
# the minimum number of internal nodes = log(n+1)-1
# the minimum number of external nodes = 1
